# 任务列表：PasteId 传参优化 — 从 string PasteId 改为 uint32_t PasteSeqId

## Implementation Tasks

- [x] 1. **IDL 文件修改**
    - [x] 1.1. 修改 IPasteboardService.idl GetPasteData 参数
        - *Goal*: 将 ipc code 200 的 `[in] String pasteId` 改为 `[in] unsigned int pasteSeqId`
        - *Details*: 仅修改 GetPasteData，PasteStart(202) 和 PasteComplete(203) 不变
        - *Requirements*: FR-1
        - *文件*: `foundation/distributeddatamgr/pasteboard/services/IPasteboardService.idl:49-50`

- [x] 2. **PasteData 类修改**
    - [x] 2.1. 删除 IsValidPasteId 及相关常量
        - *Goal*: 删除 IsValidPasteId() 声明、实现、以及仅它使用的常量 SUB_PASTEID_NUM 和 PASTEID_MAX_SIZE
        - *Details*: paste_data.h 删除 line 142 声明；paste_data.cpp 删除 line 62 SUB_PASTEID_NUM、line 63 PASTEID_MAX_SIZE、lines 875-898 实现
        - *Requirements*: FR-3
        - *文件*: `paste_data.h:142`, `paste_data.cpp:62-63,875-898`
    - [x] 2.2. 新增 CreatePasteId 三参数版本
        - *Goal*: 新增 `static std::string CreatePasteId(const std::string &name, uint32_t sequence, pid_t pid);` 供 Service 端传入 callingPid
        - *Details*: paste_data.h 新增声明；paste_data.cpp 新增实现（逻辑与原版相同，仅 pid 由参数传入而非 getpid()）。原两参数版本保留。
        - *Requirements*: FR-3
        - *文件*: `paste_data.h`, `paste_data.cpp`

- [x] 3. **PasteboardClient 修改（client 端核心）**
    - [x] 3.1. PasteDataFromServiceInfo 结构体变更
        - *Goal*: `std::string currentId` → `uint32_t currentSeqId`
        - *Details*: pasteboard_client.h line 53 改类型；所有引用 currentId 的地方改为 currentSeqId
        - *Requirements*: FR-2
        - *文件*: `pasteboard_client.h:53`, `pasteboard_client.cpp`
    - [x] 3.2. GetPasteData 方法重构
        - *Goal*: 不再调用 CreatePasteId，改用 getSequenceId_++ 生成 seqId，传 uint32_t 给 IPC
        - *Details*: 删除 `std::string currentId = PasteData::CreatePasteId(...)` 和 `std::string currentPid = std::to_string(pid)`；改为 `uint32_t seqId = getSequenceId_++`；IPC 调用参数改为 seqId；radar CONCURRENT_ID 改为 `std::to_string(seqId)`；删除对 currentPid 变量的使用（radar 中 PACKAGE_NAME 可用 `std::to_string(getpid())`）
        - *Requirements*: FR-2
        - *文件*: `pasteboard_client.cpp:289-327`
    - [x] 3.3. GetDataReport 方法适配
        - *Goal*: 参数从 `const std::string &currentId` 改为 `uint32_t currentSeqId`，radar 上报中 currentId 改为 `std::to_string(currentSeqId)`
        - *Details*: 声明和实现中参数类型变更；所有 RADAR_REPORT 中使用 currentId 的地方改为 std::to_string(currentSeqId)；保留 currentPid 参数
        - *Requirements*: FR-2
        - *文件*: `pasteboard_client.h:603-604`, `pasteboard_client.cpp:329-361`
    - [x] 3.4. ProgressRadarReport 方法重构
        - *Goal*: currentId → currentSeqId，删除 pasteData.SetPasteId() 调用
        - *Details*: `pasteDataFromServiceInfo.currentSeqId = getSequenceId_++`；删除 `pasteData.SetPasteId(pasteDataFromServiceInfo.currentId)`；radar 上报改为 std::to_string(currentSeqId)
        - *Requirements*: FR-2
        - *文件*: `pasteboard_client.cpp:556-566`
    - [x] 3.5. GetPasteDataFromService 方法适配
        - *Goal*: IPC 参数从 string pasteId 改为 uint32_t pasteDataFromServiceInfo.currentSeqId
        - *Details*: 删除 `std::string pasteId = pasteDataFromServiceInfo.currentId`；proxyService->GetPasteData 参数改为 currentSeqId；radar 中 currentId 改为 std::to_string(currentSeqId)
        - *Requirements*: FR-2
        - *文件*: `pasteboard_client.cpp:444-479`
    - [x] 3.6. ProcessRadarReport 方法适配
        - *Goal*: radar 上报中 pasteDataFromServiceInfo.currentId 改为 std::to_string(pasteDataFromServiceInfo.currentSeqId)
        - *Details*: 所有 RADAR_REPORT 中使用 pasteDataFromServiceInfo.currentId 的地方改为 std::to_string(pasteDataFromServiceInfo.currentSeqId)
        - *Requirements*: FR-2
        - *文件*: `pasteboard_client.cpp:522-554`

- [ ] 4. **PasteboardService 修改（service 端核心）**
    - [x] 4.1. GetPasteData 方法签名和逻辑变更
        - *Goal*: 参数从 `const std::string &pasteId` 改为 `uint32_t pasteSeqId`；删除 IsValidPasteId 校验；入口拼接完整 pasteId
        - *Details*: 签名 `uint32_t pasteSeqId`；删除 `IsValidPasteId(pasteId)` 检查；新增 `std::string pasteId = PasteData::CreatePasteId("GetPasteData", pasteSeqId, IPCSkeleton::GetCallingPid())`；后续逻辑不变（传 pasteId 给 GetPasteDataInner）
        - *Requirements*: FR-3
        - *文件*: `pasteboard_service.h:133-134`, `pasteboard_service.cpp:1301-1332`
    - [x] 4.2. GetPasteDataInner 方法签名变更
        - *Goal*: 参数从 `const std::string &pasteId` 改为 `uint32_t pasteSeqId`；内部在入口处拼接 pasteId
        - *Details*: 签名 `uint32_t pasteSeqId`；入口 `std::string pasteId = PasteData::CreatePasteId("GetPasteData", pasteSeqId, IPCSkeleton::GetCallingPid())`；data.SetPasteId(pasteId)；后续逻辑不变
        - *Requirements*: FR-3
        - *文件*: `pasteboard_service.h:366-367`, `pasteboard_service.cpp:1334-1377`
    - [x] 4.3. CheckAndGrantRemoteUri 方法签名变更
        - *Goal*: 参数从 `const std::string &pasteId` 改为 `uint32_t pasteSeqId`；内部拼接 pasteId
        - *Details*: 签名 `uint32_t pasteSeqId`；入口拼接 pasteId；内部使用不变
        - *Requirements*: FR-3
        - *文件*: `pasteboard_service.h:349-350`, `pasteboard_service.cpp:1438-1461`

- [ ] 5. **Error Handler 修改**
    - [x] 5.1. 修改错误描述字符串
        - *Goal*: `"Invalid pasteId parameter"` → `"Invalid pasteSeqId parameter"`
        - *Requirements*: FR-5
        - *文件*: `foundation/distributeddatamgr/pasteboard/tools/ohos-pasteboard/src/error_handler.cpp`

- [x] 6. **测试适配**
    - [x] 6.1. 单元测试 paste_data_test.cpp
        - *Goal*: 删除 IsValidPasteId 测试用例，新增 CreatePasteId 三参数版本测试
        - *Details*: 删除 4 个 IsValidPasteId 测试；新增测试验证三参数版本正确拼接 callingPid
        - *Requirements*: FR-3
        - *文件*: `foundation/distributeddatamgr/pasteboard/framework/test/src/paste_data_test.cpp`
    - [x] 6.2. 单元测试 pasteboard_client_test.cpp
        - *Goal*: 适配 PasteDataFromServiceInfo 结构体变更
        - *Details*: currentId → currentSeqId
        - *Requirements*: FR-2
        - *文件*: `foundation/distributeddatamgr/pasteboard/framework/test/src/pasteboard_client_test.cpp`
    - [x] 6.3. 单元测试 pasteboard_service_mock_test.cpp
        - *Goal*: GetPasteData 参数从 string 改为 uint32_t
        - *Details*: Mock 和测试用例中 GetPasteData 参数适配
        - *Requirements*: FR-3
        - *文件*: `foundation/distributeddatamgr/pasteboard/services/test/unittest/src/pasteboard_service_mock_test.cpp`
    - [x] 6.4. Fuzz 测试适配
        - *Goal*: 删除 IsValidPasteId fuzz，适配 GetPasteData 参数变更
        - *Details*: pasteboarddata_fuzzer 删除 IsValidPasteId；pasteboardconcurrent_fuzzer GetPasteData 参数改为 uint32_t；pasteboardservice_fuzzer 适配 stub
        - *Requirements*: FR-3
        - *文件*: `foundation/distributeddatamgr/pasteboard/test/fuzztest/` 下多个文件

## Task Dependencies

- Task 2 (PasteData) 必须先完成，Task 3/4 依赖其 CreatePasteId 三参数版本和 IsValidPasteId 删除
- Task 1 (IDL) 和 Task 2 可并行执行
- Task 3 (Client) 和 Task 4 (Service) 可并行执行，但都依赖 Task 2
- Task 5 (Error Handler) 可独立执行
- Task 6 (测试) 依赖 Task 1-5 全部完成

## Estimated Timeline

- Task 1: 0.5 小时
- Task 2: 1 小时
- Task 3: 2 小时
- Task 4: 1.5 小时
- Task 5: 0.5 小时
- Task 6: 1.5 小时
- **总计: 7 小时**
