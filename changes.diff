diff --git a/services/core/include/pasteboard_service.h b/services/core/include/pasteboard_service.h
index 7b112338..2eb1ffba 100644
--- a/services/core/include/pasteboard_service.h
+++ b/services/core/include/pasteboard_service.h
@@ -193,7 +193,7 @@ public:
     int32_t GetCurrentAccountId() const;
     UserContext ResolveEventUser(const EventFwk::CommonEventData &data) const;
     UserContext ResolveUserIdFromWant(const AAFwk::Want &want) const;
-    std::vector<int32_t> GetForegroundUserIds() const;
+    std::vector<UserContext> ResolveForegroundUsers() const;
     int32_t ResolveMainDisplayUserId() const;
     void ClearByResolvedUser(int32_t userId);
     int32_t ClearByEventUser(int32_t userId);
diff --git a/services/core/include/pasteboard_user_context.h b/services/core/include/pasteboard_user_context.h
index e9608ecf..a4d94653 100644
--- a/services/core/include/pasteboard_user_context.h
+++ b/services/core/include/pasteboard_user_context.h
@@ -66,7 +66,6 @@ public:
     virtual UserContext ResolveStoppingUser(const EventFwk::CommonEventData &data) const;
     virtual UserContext ResolveUserIdFromWant(const AAFwk::Want &want) const;
     virtual UserContext ResolveInteractionUser(int32_t userId) const;
-    virtual std::vector<int32_t> GetForegroundUserIds() const;
 
 private:
     UserContext MakeEventContext(int32_t userId, UserContextSource source) const;
diff --git a/services/core/src/pasteboard_service.cpp b/services/core/src/pasteboard_service.cpp
index 2a65121b..f2f8e3df 100644
--- a/services/core/src/pasteboard_service.cpp
+++ b/services/core/src/pasteboard_service.cpp
@@ -165,18 +165,21 @@ void PasteboardService::InitScreenStatus()
     auto screenLockManager = OHOS::ScreenLock::ScreenLockManager::GetInstance();
     PASTEBOARD_CHECK_AND_RETURN_LOGE(screenLockManager != nullptr, PASTEBOARD_MODULE_SERVICE,
         "ScreenLockManager instance is null.");
-    auto foregroundUsers = GetForegroundUserIds();
-    for (auto userId : foregroundUsers) {
+    auto foregroundUsers = ResolveForegroundUsers();
+    for (const auto &ctx : foregroundUsers) {
+        if (!ctx.isValid) {
+            continue;
+        }
         bool isLocked = false;
-        auto ret = screenLockManager->IsLockedWithUserId(userId, isLocked);
+        auto ret = screenLockManager->IsLockedWithUserId(ctx.userId, isLocked);
         if (ret == ERR_OK) {
-            screenStatusMap_.InsertOrAssign(userId,
+            screenStatusMap_.InsertOrAssign(ctx.userId,
                 isLocked ? ScreenEvent::ScreenLocked : ScreenEvent::ScreenUnlocked);
             PASTEBOARD_HILOGI(PASTEBOARD_MODULE_SERVICE, "screen status userId=%{public}d is %{public}d",
-                userId, isLocked ? ScreenEvent::ScreenLocked : ScreenEvent::ScreenUnlocked);
+                ctx.userId, isLocked ? ScreenEvent::ScreenLocked : ScreenEvent::ScreenUnlocked);
         } else {
             PASTEBOARD_HILOGE(PASTEBOARD_MODULE_SERVICE,
-                "IsLockedWithUserId failed, userId=%{public}d, ret=%{public}d", userId, ret);
+                "IsLockedWithUserId failed, userId=%{public}d, ret=%{public}d", ctx.userId, ret);
         }
     }
 #else
@@ -2993,13 +2996,13 @@ UserContext PasteboardService::ResolveUserIdFromWant(const AAFwk::Want &want) co
     return userContextResolver_->ResolveUserIdFromWant(want);
 }
 
-std::vector<int32_t> PasteboardService::GetForegroundUserIds() const
+std::vector<UserContext> PasteboardService::ResolveForegroundUsers() const
 {
     if (userContextResolver_ == nullptr) {
         PASTEBOARD_HILOGE(PASTEBOARD_MODULE_SERVICE, "resolver is null.");
         return {};
     }
-    return userContextResolver_->GetForegroundUserIds();
+    return userContextResolver_->ResolveForegroundUsers();
 }
 
 int32_t PasteboardService::ResolveMainDisplayUserId() const
@@ -3605,16 +3608,16 @@ std::string PasteboardService::DumpUserHistory(int32_t userId) const
 
 std::string PasteboardService::DumpHistory() const
 {
-    auto foregroundUsers = GetForegroundUserIds();
+    auto foregroundUsers = ResolveForegroundUsers();
     PASTEBOARD_CHECK_AND_RETURN_RET_LOGE(!foregroundUsers.empty(), "Access history fail! no foreground user.",
         PASTEBOARD_MODULE_SERVICE, "no foreground user");
     std::string result;
-    for (const auto userId : foregroundUsers) {
-        if (userId == ERROR_USERID) {
+    for (const auto &ctx : foregroundUsers) {
+        if (ctx.userId == ERROR_USERID) {
             continue;
         }
-        result += "UserId: " + std::to_string(userId) + "\n";
-        result += DumpUserHistory(userId);
+        result += "UserId: " + std::to_string(ctx.userId) + "\n";
+        result += DumpUserHistory(ctx.userId);
     }
     return result;
 }
@@ -3647,18 +3650,18 @@ std::string PasteboardService::DumpUserData(int32_t userId)
 
 std::string PasteboardService::DumpData()
 {
-    auto foregroundUsers = GetForegroundUserIds();
+    auto foregroundUsers = ResolveForegroundUsers();
     if (foregroundUsers.empty()) {
         PASTEBOARD_HILOGE(PASTEBOARD_MODULE_SERVICE, "query foreground users failed.");
         return "";
     }
     std::string result;
-    for (const auto userId : foregroundUsers) {
-        if (userId == ERROR_USERID) {
+    for (const auto &ctx : foregroundUsers) {
+        if (ctx.userId == ERROR_USERID) {
             continue;
         }
-        result += "UserId: " + std::to_string(userId) + "\n";
-        result += DumpUserData(userId);
+        result += "UserId: " + std::to_string(ctx.userId) + "\n";
+        result += DumpUserData(ctx.userId);
     }
     return result;
 }
diff --git a/services/core/src/pasteboard_user_context.cpp b/services/core/src/pasteboard_user_context.cpp
index 96adf2b6..bdef6355 100644
--- a/services/core/src/pasteboard_user_context.cpp
+++ b/services/core/src/pasteboard_user_context.cpp
@@ -134,17 +134,6 @@ UserContext UserContextResolver::ResolveInteractionUser(int32_t userId) const
     return context;
 }
 
-std::vector<int32_t> UserContextResolver::GetForegroundUserIds() const
-{
-    std::vector<int32_t> accountIds;
-    auto ret = AccountSA::OsAccountManager::QueryActiveOsAccountIds(accountIds);
-    if (ret != ERR_OK || accountIds.empty()) {
-        PASTEBOARD_HILOGE(PASTEBOARD_MODULE_SERVICE, "query active user failed errCode=%{public}d", ret);
-        return {};
-    }
-    return accountIds;
-}
-
 bool IsMainScreenUser(int32_t userId)
 {
     return userId == MAIN_SCREEN_USER_ID;
